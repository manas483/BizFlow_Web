import * as fs from 'fs';
import { parseInvoicePdfLocally } from './pdf-parser';

// create a dummy db mock if possible, but actually we can just pass the config manually if we expose extractProductRows.
