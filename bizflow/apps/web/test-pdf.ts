import { parseInvoicePdfLocally } from './src/shared/lib/pdf-parser'; import * as fs from 'fs'; import { findProductIntelligence } from './src/shared/lib/business-intelligence'; async function run() { const buffer = fs.readFileSync('C:/Users/sacha/Downloads/Sales_ASDBPDS0434.pdf'); const result = await parseInvoicePdfLocally(buffer, 'test'); const p = result.products[0]; const intel = findProductIntelligence('Fertilizer Dealer', p.name, p.sku, p.hsnCode); console.log('Parsed Category:', intel?.category || 'Other'); } run();
