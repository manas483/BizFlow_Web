import 'dotenv/config'; import { InventoryService } from '../src/modules/inventory'; async function main() { try { const session = { user: { id: 'test_user', name: 'Test User', businessId: 'cmqpggqbl000005ier4oy5qfs' } }; const product = await InventoryService.getProductById('cmqsadrhu000004lbarvjlxan', session.user.businessId); if (!product) return console.log('not found'); await InventoryService.deleteProduct('cmqsadrhu000004lbarvjlxan', product, session); console.log('Deleted successfully'); } catch (e) { console.error('Error during delete:', e); } } main().finally(() => process.exit(0));
